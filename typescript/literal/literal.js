"use strict";
let c2 = 'test';
function performAction(action) {
    switch (action) {
        case 'down':
            return -1;
        case 'up':
            return 1;
    }
}
function performAction1(action) {
    switch (action) {
        case 'down':
            return -1;
        case 'up':
            return 1;
    }
}
